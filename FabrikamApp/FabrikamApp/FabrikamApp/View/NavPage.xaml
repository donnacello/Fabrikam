<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FabrikamApp.View.NavPage">
  <ContentPage.Padding>
    <OnPlatform x:TypeArguments="Thickness" iOS="0, 20, 0, 0" />
  </ContentPage.Padding>
  <ContentPage.Content>
    <StackLayout BackgroundColor="Transparent">
      <Label Text=" "/>
      <Image Source="fabrikam_logo.png" HeightRequest="80"/>
      <Label Text=" "/>
      <Button Text="Home" TextColor="White" BackgroundColor="Green" Command="{Binding GoHomeCommand}" />
      <Button Text="Menu" TextColor="White" BackgroundColor="Green" Command="{Binding GoMenuCommand}" />
      <Button Text="Suggestions" TextColor="White" BackgroundColor="Red" Command="{Binding GoSuggestionsCommand}" />
      <Button Text="Contact Us" TextColor="White" BackgroundColor="Green" Command="{Binding GoContactCommand}" />
    </StackLayout>
  </ContentPage.Content>
</ContentPage>